.DS_Store
node_modules/
main.js
*.log
data.json
.obsidian/

# Test coverage output (regenerated via `npm run coverage`).
coverage/

# Internal design notes — kept locally, excluded from the public repo.
agent_notes/

# Phase 0 smoke test lives in its own throwaway repo.
smoke-test/
